# 크롬 익스텐션 개발 기획서

> **프로젝트명:** QuickSave — 퀵 투두 & 링크 저장소
> **작성일:** 2026년 5월
> **제출 기한:** 21일 6교시
> **사용 기술:** HTML / CSS / JavaScript

---

## ① 서비스 정의 — 무엇을, 누구를 위해 만드는가?

### 1-1. 서비스 개요

웹 서핑 중 발생하는 두 가지 불편함을 하나의 익스텐션으로 해결한다.

- 나중에 읽어야 할 링크 → 따로 저장해두지 않으면 잃어버림
- 해야 할 일 → 메모앱에 따로 적어두면 링크와 연결이 안 됨

**QuickSave**는 우클릭 한 번으로 현재 페이지를 **"할 일"** 또는 **"LATER"** 로 즉시 저장하고,
팝업 UI에서 한눈에 관리할 수 있는 크롬 익스텐션이다.

### 1-2. 타겟 사용자

| 대상        | 불편한 상황                                                |
| ----------- | ---------------------------------------------------------- |
| 대학생      | 과제 관련 자료 탭을 수십 개 열어두다 잃어버림              |
| 직장인      | 업무 중 참고 링크를 북마크에 저장하지만 나중에 찾기 어려움 |
| 일반 사용자 | 읽을 거리를 발견했지만 지금 당장 읽을 시간이 없음          |

### 1-3. 기대 효과

- 탭 과부하 감소
- 할 일과 참고 링크를 한 곳에서 통합 관리
- 우클릭만으로 즉시 저장 → 작업 흐름 방해 최소화

---

## ② 기능 정의 — 어떤 기능이 필요한가?

### 2-1. 핵심 기능 (필수)

| 기능                     | 설명                                                                                    |
| ------------------------ | --------------------------------------------------------------------------------------- |
| **우클릭 컨텍스트 메뉴** | 페이지에서 우클릭 시 "할 일로 저장" / "LATER로 저장" 메뉴 표시                          |
| **드래그 선택 → 메모 자동입력** | 텍스트 선택 후 우클릭 저장 시 선택된 텍스트가 메모 칸에 자동 채워짐                |
| **데이터 저장**          | 페이지 제목, URL, 저장 날짜를 `chrome.storage.local`에 저장                             |
| **팝업 UI**              | 익스텐션 아이콘 클릭 시 저장된 항목을 탭으로 구분하여 별도 창으로 표시                  |
| **실시간 팝업 업데이트** | 우클릭 저장 즉시 팝업 창 목록에 반영 (새로고침 불필요)                                  |
| **빠른 할 일 추가**      | TODO 탭 상단 입력창에서 URL 없이 텍스트만으로 할 일을 즉시 추가 (Enter 또는 + 버튼)    |
| **메모 붙이기**          | 저장된 항목에 짧은 메모를 편집창에서 입력·수정                                          |
| **태그 분류 + 필터**     | #태그를 붙여 저장, 팝업에서 태그별 필터링 가능                                          |
| **Windows 알림**         | 편집 모달에서 원하는 날짜·시각을 직접 지정하면 해당 시각에 Windows 알림 발생           |
| **뱃지 — 미완료 TODO 수** | 아이콘 뱃지에 완료되지 않은 TODO 개수를 보라색 배경으로 표시                           |
| **항목 고정(Pin)**       | 항목을 핀 고정하면 목록 최상단에 고정, 보라 왼쪽 테두리로 시각적 구분                  |
| **검색 기능**            | 팝업 내 검색창으로 제목/메모/태그 실시간 검색 (전 탭 통합)                              |
| **메모/태그 편집**       | 저장된 항목의 편집 버튼으로 메모·태그·알림 일시를 언제든 수정 가능                     |
| **완료 체크**            | TODO와 LATER 모두 체크박스 제공, 완료 시 취소선 표시                                   |
| **단축키 팝업 열기**     | Alt+Shift+Q 로 어느 페이지에서든 팝업 즉시 호출                                         |
| **최소 창 크기 제한**    | 상하좌우 모두 최소 크기(376×400px) 이하로 축소 불가                                     |
| **드래그 순서 변경**     | TODO / LATER 탭에서 드래그&드롭으로 항목 우선순위 직접 정렬, 고정 항목은 고정 영역 내에서만 이동 |
| **파비콘 표시**          | 각 항목 제목 옆에 사이트 파비콘 표시 (Google Favicons API 사용, 로드 실패 시 자동 숨김) |
| **데이터 내보내기/가져오기** | JSON 파일로 전체 데이터 백업·복원, 가져오기 시 중복 ID는 자동 스킵              |
| **주간 통계**            | 설정 모달에서 이번 주 TODO 저장 수·완료 수·완료율 확인 (LATER 제외)                   |

### 2-2. 세부 기능

#### 우클릭 메뉴

- 메뉴 항목 1: **"할 일로 저장"** → 현재 페이지를 TODO 목록에 추가
- 메뉴 항목 2: **"LATER로 저장"** → 현재 페이지를 LATER 목록에 추가
- 중복 URL이면 저장 대신 토스트 알림 표시
- 텍스트 드래그 선택 후 저장 시 `info.selectionText`가 메모에 자동 채워짐

#### 데이터 저장 항목

- 페이지 제목 (title)
- 페이지 URL (없으면 빈 문자열 — 빠른 추가 항목)
- 저장 날짜 (ko-KR 형식)
- 완료 여부 (done) — TODO·LATER 공통, 기본값: false
- 메모 (memo) — 사용자가 직접 입력한 짧은 메모
- 태그 (tags) — 배열 형태로 저장 (예: ["과제", "업무"])
- 알림 일시 (alarmAt) — datetime-local 형식 문자열 (예: "2026-05-21T09:00")
- 고정 여부 (pinned) — true이면 목록 최상단 고정

#### 팝업 UI 기능

- **탭 전환:** TODO 탭 / LATER 탭 / 검색결과 탭 (검색 시 자동 생성)
- **빠른 추가:** TODO 탭 상단 입력창에서 텍스트만으로 URL 없는 할 일 추가
- **완료 체크:** TODO·LATER 항목 모두 체크박스 제공 (완료 시 취소선 표시)
- **삭제:** 각 항목별 삭제 버튼 제공 (알람도 동시 해제)
- **링크 이동:** 제목 클릭 시 해당 URL로 새 탭 열기 (URL 없는 항목은 클릭 비활성)
- **고정:** 핀 버튼 클릭으로 항목 고정·해제, 고정된 항목은 상단에 모여 있으며 보라 좌측 테두리 표시
- **전체 삭제:** 탭별 전체 삭제 버튼 (확인 모달 표시)
- **설정:** 단축키 ON/OFF 토글, 단축키 직접 변경, 데이터 내보내기/가져오기, 주간 통계

### 2-3. 기능 흐름도

```
[사용자가 웹페이지에서 우클릭]
        ↓
[컨텍스트 메뉴 표시]
   ├── "할 일로 저장" 클릭
   │       ↓
   │   background.js → chrome.storage.local 저장 (todo 배열)
   │       ↓
   │   팝업 창 열려 있으면 → storage.onChanged → 즉시 화면 반영
   │
   └── "LATER로 저장" 클릭
           ↓
       background.js → chrome.storage.local 저장 (readlater 배열)
           ↓
       팝업 창 열려 있으면 → storage.onChanged → 즉시 화면 반영

[익스텐션 아이콘 클릭 또는 Alt+Shift+Q]
        ↓
[별도 팝업 창 열기 (popup.html)]
   ├── TODO 탭: 빠른 추가, 체크박스, 편집, 삭제, 고정, 링크 이동
   └── LATER 탭: 체크박스, 편집, 삭제, 고정, 링크 이동

[편집 모달에서 알림 일시 지정]
        ↓
background.js → chrome.alarms.create(when: alarmAt)
        ↓
알람 발화 시 → chrome.notifications.create() → Windows 알림 팝업
```

---

## ③ UX 설계 — 사용자는 어떻게 쓰는가?

### 3-1. 사용 시나리오

**시나리오 A — 할 일 저장**

1. 사용자가 과제 관련 페이지를 발견
2. 페이지 빈 곳에서 **우클릭**
3. 메뉴에서 **"할 일로 저장"** 클릭
4. 저장 완료 — 화면 우하단에 토스트 알림 표시
5. 열려 있는 팝업 창에 **즉시** 항목 추가됨 (새로고침 불필요)
6. 나중에 팝업을 열어 TODO 탭에서 확인 후 체크 처리

**시나리오 B — LATER 저장**

1. 흥미로운 기사를 발견했지만 지금 읽을 시간이 없음
2. 페이지에서 **우클릭 → "LATER로 저장"** 클릭
3. 팝업의 LATER 탭에서 나중에 확인
4. 제목 클릭 → 해당 페이지 새 탭으로 이동

**시나리오 C — 드래그 선택 + 메모 자동입력**

1. 중요한 문장을 마우스로 드래그 선택
2. 선택 상태에서 우클릭 → "할 일로 저장"
3. 저장된 항목의 메모 칸에 선택한 텍스트가 자동으로 채워짐

**시나리오 D — Windows 알림 설정**

1. 항목의 편집 버튼 클릭
2. "알림 일시" 입력창에서 원하는 날짜와 시각 선택
3. 저장 → 해당 시각에 Windows 알림으로 제목 표시

### 3-2. 팝업 UI 레이아웃

```
┌─────────────────────────────────────┐
│  [북마크 아이콘] QuickSave  [🔍검색]  │  ← 헤더
├─────────────────┬───────────────────┤
│      TODO       │       LATER       │  ← 탭
├─────────────────────────────────────┤
│  할 일 빠르게 추가... (Enter)   [+]  │  ← 빠른 추가 (TODO 탭에서만)
├─────────────────────────────────────┤
│ [전체] [#과제] [#업무]               │  ← 태그 필터
├─────────────────────────────────────┤
│ 📌 ☐ [📎] [페이지 제목]    [✏][🗑]  │  ← 고정 카드 (좌측 보라 테두리)
│        🔔 5/21 09:00  2026. 5. 19.  │
│ ☐ [📎] [페이지 제목]        [✏][🗑] │  ← 일반 카드
│   📝 "나중에 다시 읽기"             │
│   #과제          2026. 5. 20.       │
├─────────────────────────────────────┤
│ 전체 삭제    3개 항목    [≡] 설정   │  ← 푸터
└─────────────────────────────────────┘
```

### 3-3. UX 원칙

- **최소 클릭:** 우클릭 1번 + 메뉴 선택 1번으로 저장 완료
- **빠른 확인:** 팝업 열면 바로 목록이 보임 (로딩 없음)
- **실시간 반영:** 저장 즉시 열려 있는 팝업도 업데이트
- **직관적 구조:** TODO / LATER를 탭으로 명확히 분리
- **링크 접근성:** 제목 클릭 시 바로 해당 페이지로 이동
- **토스 스타일 디자인:** 군더더기 없는 흰색 배경 + 보라색 포인트

---

## ④ 기술 명세 — 어떤 기술로 구현하는가?

### 4-1. 사용 기술

| 기술           | 용도                                                   |
| -------------- | ------------------------------------------------------ |
| **HTML**       | 팝업 UI 구조 (popup.html)                              |
| **CSS**        | 토스 스타일 디자인 시스템 (흰색 배경, 보라색 포인트)   |
| **JavaScript** | 우클릭 메뉴, 데이터 저장/불러오기, 실시간 UI 렌더링    |
| **SVG**        | 인라인 아이콘 (북마크, 돋보기, 연필, 휴지통, 슬라이더) |

### 4-2. 파일 구조

```
QuickSave/
├── manifest.json           ← 익스텐션 설정 (권한, 단축키, 파일 등록)
├── background.js           ← 우클릭 메뉴, 데이터 저장, 창 관리, 뱃지, 알람
├── popup.html              ← 팝업 UI 구조 (SVG 아이콘 포함)
├── popup.css               ← 토스 스타일 디자인 (흰색 + 보라 #7B3FF2)
├── popup.js                ← 팝업 동작 로직 + 실시간 storage 리스너
└── icons/
    ├── icon16.png          ← 확장 프로그램 목록용 아이콘
    ├── icon48.png          ← 확장 프로그램 관리 페이지용 아이콘
    ├── icon128.png         ← 스토어 및 설치 화면용 아이콘
```

### 4-3. 크롬 API 사용 목록

| API                              | 용도                                                                     |
| -------------------------------- | ------------------------------------------------------------------------ |
| `chrome.contextMenus.create()`   | 우클릭 메뉴 항목 등록                                                    |
| `chrome.contextMenus.onClicked`  | 메뉴 클릭 이벤트 감지 (`info.selectionText`로 드래그 텍스트 수집)        |
| `chrome.storage.local.set()`     | 데이터 저장                                                              |
| `chrome.storage.local.get()`     | 저장된 데이터 불러오기                                                   |
| `chrome.storage.onChanged`       | 저장 데이터 변경 감지 → 팝업 실시간 업데이트 + 뱃지 자동 갱신           |
| `chrome.tabs.query()`            | 현재 탭의 URL, 제목 가져오기                                             |
| `chrome.runtime.onInstalled`     | 익스텐션 설치 시 초기화                                                  |
| `chrome.action.setBadgeText()`   | 미완료 TODO 개수를 아이콘 뱃지에 표시 (보라색 배경)                      |
| `chrome.commands`                | 키보드 단축키로 팝업 열기 (Alt+Shift+Q), `commands.update`로 단축키 변경 |
| `chrome.windows.create()`        | 팝업을 독립 창으로 생성                                                  |
| `chrome.windows.onBoundsChanged` | 창 크기 변경 감지 → 최소 크기(376×400px) 강제                            |
| `chrome.scripting.executeScript` | 저장 완료 시 토스트 알림 삽입                                            |
| `chrome.alarms.create()`         | 사용자가 지정한 날짜·시각에 알람 등록                                    |
| `chrome.alarms.onAlarm`          | 알람 발화 감지 → Windows 알림 생성                                       |
| `chrome.notifications.create()`  | 알람 발화 시 Windows 시스템 알림 팝업 표시                               |
| `Google Favicons API`            | `https://www.google.com/s2/favicons?domain=` — 사이트 파비콘 이미지 로드 |

### 4-4. 기술 상세

#### HTML (popup.html)

- 전체 레이아웃: 헤더 / 탭 바 / 빠른 추가 행 / 태그 필터 / 목록 / 푸터 + 3개 모달
- 이모지 대신 **인라인 SVG 아이콘** 사용 (북마크·돋보기·편집·삭제·설정)
- 모달 3종: 메모/태그/알림 편집, 설정(단축키·내보내기·통계), 전체 삭제 확인

#### CSS (popup.css)

- **디자인 시스템:** 토스(Toss) 앱 스타일 참고 — 흰색 배경, 보라색(`#7B3FF2`) 포인트
- 카드 컴포넌트: 테두리 없이 `box-shadow`로 부유감 표현
- **고정 카드:** 3px 보라 좌측 테두리로 시각적 구분 (`.pinned-card`)
- **마감 초과 카드:** 연한 붉은 테두리 (`.overdue`) — 알람 시각이 현재보다 과거인 항목
- 탭 UI: 하단 2.5px 언더라인 인디케이터 방식
- 태그: `border-radius: 100px` pill 형태, 연한 보라 배경(`#EDE9FE`)
- 모달: `border-radius: 18px`, 큰 폰트 제목(`font-weight: 800`)
- 토글 스위치: 커스텀 CSS (보라색 ON 상태)
- 스크롤바: 3px 폭 미니멀 스타일

#### JavaScript

**background.js**

- `chrome.runtime.onInstalled`에서 컨텍스트 메뉴 2개 등록 및 storage 초기화
- `chrome.contextMenus.onClicked`에서 클릭된 메뉴 구분, `info.selectionText`를 memo로 자동 채움, 중복 URL 체크 후 저장
- 서비스워커 슬립 후 재기동 시에도 팝업 창 ID를 `_winId` 키로 storage에 보존
- `chrome.windows.onBoundsChanged`로 최소 창 크기(376×400px) 강제 적용
- `chrome.storage.onChanged`에서 todo 변경 감지 → `updateBadge()` 호출 (미완료 TODO 수 뱃지)
- `setAlarm` / `clearAlarm` 메시지 처리 → `chrome.alarms`로 사용자 지정 시각에 알람 예약
- `chrome.alarms.onAlarm`에서 `chrome.notifications.create()`로 Windows 알림 표시

**popup.js**

- 팝업 로드 시 `chrome.storage.local.get()`으로 데이터 불러오기
- `chrome.storage.onChanged` 리스너로 우클릭 저장 즉시 UI 반영 (새로고침 불필요)
- TODO / LATER 동적 `<li>` 카드 생성·렌더링 (고정 항목 상단 배치, 알람 뱃지 표시)
- `bindDragEvents()`: 드래그 경계 체크 — `data-pinned` 속성 비교로 고정 항목이 비고정 영역으로 이동 불가
- `bindQuickAdd()`: URL 없는 TODO 항목을 입력창에서 즉시 추가
- `togglePin()`: 항목 고정·해제, 고정 시 배열 맨 앞으로 이동
- `deleteItem()`: 삭제 전 `clearAlarm` 메시지 전송으로 알람 자동 해제
- 검색어 입력 시 "검색결과" 탭 자동 활성화, 전 탭 통합 검색
- 설정 모달: 단축키 실시간 캡처 + background.js로 `chrome.commands.update` 전달
- `exportData()` / `importData()`: JSON 파일 내보내기·가져오기 (중복 ID 스킵)
- `renderStats()`: 이번 주 TODO 항목의 저장 수·완료 수·완료율 계산 (LATER 제외)
- 창 닫힐 때 현재 위치(screenX/Y)를 storage에 저장 → 다음 실행 시 같은 위치에 열림

### 4-5. manifest.json 권한 설정

```json
{
  "manifest_version": 3,
  "name": "QuickSave",
  "version": "1.0",
  "description": "우클릭으로 할 일과 읽기 목록을 즉시 저장하는 익스텐션",
  "permissions": ["contextMenus", "storage", "tabs", "scripting", "windows", "alarms", "notifications"],
  "background": { "service_worker": "background.js" },
  "action": {
    "default_icon": {
      "16": "icons/icon16.png",
      "48": "icons/icon48.png",
      "128": "icons/icon128.png"
    }
  },
  "icons": {
    "16": "icons/icon16.png",
    "48": "icons/icon48.png",
    "128": "icons/icon128.png"
  },
  "commands": {
    "open-popup": {
      "suggested_key": { "default": "Alt+Shift+Q" },
      "description": "QuickSave 패널 열기/닫기"
    }
  }
}
```

---

## ⑤ 디자인 가이드

### 5-1. 컬러 팔레트

| 역할           | 값        | 설명                     |
| -------------- | --------- | ------------------------ |
| Primary        | `#7B3FF2` | 보라색 — 버튼, 탭, 강조  |
| Primary Light  | `#EDE9FE` | 연보라 — 태그 배경, 호버 |
| Background     | `#F7F7FB` | 연한 보라빛 흰색 배경    |
| Surface        | `#FFFFFF` | 카드·헤더·푸터 배경      |
| Text           | `#191921` | 기본 텍스트              |
| Text Secondary | `#9098A9` | 보조 텍스트              |
| Success        | `#05C072` | 완료·저장 성공           |
| Danger         | `#F03E3E` | 삭제·오류                |
| Overdue        | `#ffb3b3` | 마감 초과 카드 테두리    |

### 5-2. 아이콘 가이드

모든 아이콘은 이모지 대신 **SVG 인라인 코드**를 사용한다.

| 위치           | 아이콘          | 형태        |
| -------------- | --------------- | ----------- |
| 로고           | 북마크 (filled) | fill=accent |
| 검색창         | 돋보기          | stroke      |
| 항목 편집 버튼 | 연필            | stroke      |
| 항목 삭제 버튼 | 휴지통          | stroke      |
| 설정 버튼      | 슬라이더 3줄    | stroke      |

익스텐션 아이콘 (PNG): `icons/generate-icons.html`을 Chrome에서 열어 다운로드.

- 보라 그라디언트 배경 + 흰색 북마크 형태
- 128px / 48px / 16px 3종

---

## 📅 개발 일정

| 단계  | 작업 내용                                                |
| ----- | -------------------------------------------------------- |
| 1단계 | 기획서 작성, manifest.json 및 파일 구조 세팅             |
| 2단계 | background.js — 우클릭 메뉴, 저장 로직, 뱃지             |
| 3단계 | popup.html / popup.css / popup.js — UI 구현              |
| 4단계 | 디자인 개선 (토스 스타일), SVG 아이콘 교체               |
| 5단계 | 실시간 업데이트, 최소 창 크기 강제, 서비스워커 슬립 대응 |
| 6단계 | 드래그 정렬, 고정(Pin), 알람, 통계, 내보내기/가져오기   |
| 7단계 | 전체 테스트, 버그 수정, 최종 제출                        |

---

## 📝 변경 이력

| 날짜       | 변경 내용                                                                                           |
| ---------- | --------------------------------------------------------------------------------------------------- |
| 2026-05-19 | 우클릭 저장 즉시 팝업 반영 — `chrome.storage.onChanged` 리스너 추가 (popup.js)                      |
| 2026-05-20 | 디자인 전면 개편 — 다크→화이트 테마, 토스 스타일, 보라 포인트 컬러 (#7B3FF2)                        |
| 2026-05-20 | 이모지 → SVG 아이콘 교체 — 로고·검색·편집·삭제·설정 전부 인라인 SVG로 변경                          |
| 2026-05-20 | 최소 창 크기 강제 개선 — 서비스워커 슬립 시 winId 소실 버그 수정, `_winId` storage 보존으로 해결    |
| 2026-05-20 | 최소 창 크기 조정 — WIDTH 376px, HEIGHT 400px (상하좌우 전방향)                                     |
| 2026-05-20 | 아이콘 생성기 추가 — `icons/generate-icons.html` (Canvas 기반, 북마크 디자인)                       |
| 2026-05-20 | 드래그 순서 변경 추가 — TODO / LATER 탭에서 드래그&드롭으로 항목 순서 재정렬, storage 즉시 반영     |
| 2026-05-20 | 파비콘 표시 추가 — 각 항목에 Google Favicons API로 사이트 아이콘 표시, 로드 실패 시 자동 숨김       |
| 2026-05-20 | content.js 제거 — 플로팅 패널 방식 제거, 별도 창 방식으로 확정                                      |
| 2026-05-21 | 드래그 선택 → 메모 자동입력 — `info.selectionText`를 memo 필드에 자동 채움                          |
| 2026-05-21 | Windows 알림 추가 — `chrome.alarms` + `chrome.notifications` 연동, 사용자가 날짜·시각 직접 지정     |
| 2026-05-21 | 빠른 할 일 추가 — TODO 탭 상단 입력창으로 URL 없는 항목 즉시 추가                                   |
| 2026-05-21 | 항목 고정(Pin) 추가 — 핀 버튼으로 상단 고정, 보라 왼쪽 테두리 표시                                  |
| 2026-05-21 | LATER 완료 체크 추가 — LATER 탭 항목에도 체크박스·취소선 기능 추가                                  |
| 2026-05-21 | 뱃지 개선 — 탭 기반 → 미완료 TODO 수 기반으로 변경, `storage.onChanged`로 항시 갱신                 |
| 2026-05-21 | 데이터 내보내기/가져오기 추가 — JSON 파일로 백업·복원, 중복 ID 자동 스킵                            |
| 2026-05-21 | 주간 통계 추가 — 설정 모달에 이번 주 TODO 저장·완료·완료율 표시 (LATER 제외)                        |
| 2026-05-21 | 드래그 경계 강제 — 고정 항목이 비고정 영역으로 내려가지 않도록 `data-pinned` 기반 경계 체크 추가    |
| 2026-05-21 | 이름 변경 — "Read it later" → "LATER" (탭, 알림, 라벨 전체 적용)                                   |

---

_본 기획서는 개발 진행에 따라 수정될 수 있습니다._
