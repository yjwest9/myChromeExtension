# 📋 크롬 익스텐션 개발 기획서

> **프로젝트명:** QuickSave — 퀵 투두 & 링크 저장소  
> **작성일:** 2026년 5월  
> **제출 기한:** 21일 6교시  
> **사용 기술:** HTML / CSS / JavaScript

---

## ① 서비스 정의 — 무엇을, 누구를 위해 만드는가?

### 1-1. 서비스 개요

웹 서핑 중 발생하는 두 가지 불편함을 하나의 익스텐션으로 해결한다.

- 나중에 읽어야 할 링크 → 따로 저장해두지 않으면 잃어버림
- 해야 할 일 → 메모앱에 따로 적어두면 링크와 연결이 안 됨

**QuickSave**는 우클릭 한 번으로 현재 페이지를 **"할 일"** 또는 **"나중에 읽기"** 로 즉시 저장하고,
팝업 UI에서 한눈에 관리할 수 있는 크롬 익스텐션이다.

### 1-2. 타겟 사용자

| 대상        | 불편한 상황                                                |
| ----------- | ---------------------------------------------------------- |
| 대학생      | 과제 관련 자료 탭을 수십 개 열어두다 잃어버림              |
| 직장인      | 업무 중 참고 링크를 북마크에 저장하지만 나중에 찾기 어려움 |
| 일반 사용자 | 읽을 거리를 발견했지만 지금 당장 읽을 시간이 없음          |

### 1-3. 기대 효과

- 탭 과부하 감소
- 할 일과 참고 링크를 한 곳에서 통합 관리
- 우클릭만으로 즉시 저장 → 작업 흐름 방해 최소화

---

## ② 기능 정의 — 어떤 기능이 필요한가?

### 2-1. 핵심 기능 (필수)

| 기능                     | 설명                                                                 |
| ------------------------ | -------------------------------------------------------------------- |
| **우클릭 컨텍스트 메뉴** | 페이지에서 우클릭 시 "할 일로 저장" / "나중에 읽기로 저장" 메뉴 표시 |
| **데이터 저장**          | 페이지 제목, URL, 저장 날짜를 `chrome.storage.local`에 저장          |
| **팝업 UI**              | 익스텐션 아이콘 클릭 시 저장된 항목을 탭으로 구분하여 표시           |
| **메모 붙이기**          | 저장 시 짧은 메모를 함께 입력하여 저장 이유 기록                     |
| **태그 분류 + 필터**     | #태그를 붙여 저장, 팝업에서 태그별 필터링 가능                       |
| **저장 여부 뱃지 표시**  | 이미 저장한 페이지 방문 시 아이콘에 ✓ 뱃지 표시                      |
| **검색 기능**            | 팝업 내 검색창으로 제목/메모/태그 실시간 검색                        |
| **메모/태그 편집**       | 저장된 항목의 ✏️ 버튼으로 메모와 태그를 언제든 수정 가능             |
| **팝업 이동**            | 상단 핸들을 드래그하여 팝업 위치 자유롭게 이동                       |
| **단축키 팝업 열기**     | Alt+Shift+Q 로 어느 페이지에서든 팝업 즉시 호출                      |

### 2-2. 세부 기능

#### 우클릭 메뉴

- 메뉴 항목 1: **"✅ 할 일로 저장"** → 현재 페이지를 TODO 목록에 추가
- 메뉴 항목 2: **"📌 나중에 읽기로 저장"** → 현재 페이지를 읽기 목록에 추가

#### 데이터 저장 항목

- 페이지 제목 (title)
- 페이지 URL
- 저장 날짜 및 시간
- 완료 여부 (TODO 항목만 해당, 기본값: false)
- 메모 (memo) - 사용자가 직접 입력한 짧은 메모
- 태그 (tags) - 배열 형태로 저장 (예: ["과제", "업무"])

#### 팝업 UI 기능

- **탭 전환:** TODO 탭 / 읽기 목록 탭
- **완료 체크:** TODO 항목에 체크박스 제공 (완료 시 취소선 표시)
- **삭제:** 각 항목별 삭제 버튼 제공
- **링크 이동:** 항목 클릭 시 해당 URL로 새 탭 열기
- **전체 삭제:** 탭별 전체 삭제 버튼 제공

### 2-3. 기능 흐름도

```
[사용자가 웹페이지에서 우클릭]
        ↓
[컨텍스트 메뉴 표시]
   ├── "할 일로 저장" 클릭
   │       ↓
   │   background.js → chrome.storage.local 저장 (todo 배열)
   │
   └── "나중에 읽기로 저장" 클릭
           ↓
       background.js → chrome.storage.local 저장 (readlater 배열)

[익스텐션 아이콘 클릭]
        ↓
[팝업 UI 표시 (popup.html)]
   ├── TODO 탭: 체크박스, 삭제, 링크 이동
   └── 읽기 목록 탭: 삭제, 링크 이동
```

---

## ③ UX 설계 — 사용자는 어떻게 쓰는가?

### 3-1. 사용 시나리오

**시나리오 A — 할 일 저장**

1. 사용자가 과제 관련 페이지를 발견
2. 페이지 빈 곳에서 **우클릭**
3. 메뉴에서 **"✅ 할 일로 저장"** 클릭
4. 저장 완료 (별도 입력창 없이 즉시 저장)
5. 나중에 팝업을 열어 TODO 탭에서 확인 후 체크 처리

**시나리오 B — 나중에 읽기 저장**

1. 흥미로운 기사를 발견했지만 지금 읽을 시간이 없음
2. 페이지에서 **우클릭 → "📌 나중에 읽기로 저장"** 클릭
3. 팝업의 읽기 목록 탭에서 나중에 확인

### 3-2. 팝업 UI 레이아웃

```
┌─────────────────────────────┐
│  📎 QuickSave  [🔍 검색창]  │
├──────────────┬──────────────┤
│   ✅ TODO    │  📌 읽기 목록 │
├─────────────────────────────┤
│ [#과제] [#업무] [#전체]      │  ← 태그 필터
├─────────────────────────────┤
│ ☐ [페이지 제목]       [🗑️] │
│   memo: "3장까지 읽기"       │
│   #과제 · 05/18             │
├─────────────────────────────┤
│          [전체 삭제]         │
└─────────────────────────────┘
```

### 3-3. UX 원칙

- **최소 클릭:** 우클릭 1번 + 메뉴 선택 1번으로 저장 완료
- **빠른 확인:** 팝업 열면 바로 목록이 보임 (로딩 없음)
- **직관적 구조:** TODO / 읽기 목록을 탭으로 명확히 분리
- **링크 접근성:** 제목 클릭 시 바로 해당 페이지로 이동

---

## ④ 기술 명세 — 어떤 기술로 구현하는가?

### 4-1. 사용 기술

| 기술           | 용도                                                   |
| -------------- | ------------------------------------------------------ |
| **HTML**       | 팝업 UI 구조 (popup.html)                              |
| **CSS**        | 팝업 스타일링, 탭 전환 효과, 체크박스 디자인           |
| **JavaScript** | 우클릭 메뉴 처리, 데이터 저장/불러오기, UI 동적 렌더링 |

### 4-2. 파일 구조

```
QuickSave/
├── manifest.json       ← 익스텐션 설정 파일 (권한, 파일 등록)
├── background.js       ← 우클릭 메뉴 생성 및 데이터 저장 처리
├── popup.html          ← 팝업 UI 구조
├── popup.css           ← 팝업 스타일
├── popup.js            ← 팝업 동작 로직 (데이터 불러오기, 렌더링)
└── icon.png            ← 익스텐션 아이콘
```

### 4-3. 크롬 API 사용 목록

| API                             | 용도                                   |
| ------------------------------- | -------------------------------------- |
| `chrome.contextMenus.create()`  | 우클릭 메뉴 항목 등록                  |
| `chrome.contextMenus.onClicked` | 메뉴 클릭 이벤트 감지                  |
| `chrome.storage.local.set()`    | 데이터 저장                            |
| `chrome.storage.local.get()`    | 저장된 데이터 불러오기                 |
| `chrome.tabs.query()`           | 현재 탭의 URL, 제목 가져오기           |
| `chrome.runtime.onInstalled`    | 익스텐션 설치 시 초기화                |
| `chrome.action.setBadgeText()`  | 저장된 페이지 방문 시 아이콘 뱃지 표시 |
| `chrome.tabs.onActivated`       | 탭 전환 감지 → 뱃지 자동 업데이트      |
| `chrome.commands` (manifest)    | 키보드 단축키로 팝업 열기 등록         |

### 4-4. 기술 상세 — HTML / CSS / JavaScript

#### HTML (popup.html)

- 팝업 전체 레이아웃 구성
- 탭 버튼 (TODO / 읽기 목록) 마크업
- 목록 컨테이너 (`<ul>` 태그) 구조 정의
- 전체 삭제 버튼 배치

#### CSS (popup.css)

- 팝업 고정 크기 설정 (width: 360px)
- 탭 활성/비활성 스타일 전환
- 완료 항목 취소선 스타일 (`.done { text-decoration: line-through; }`)
- 삭제 버튼 hover 효과
- 스크롤 가능한 목록 영역

#### JavaScript

**background.js**

- `chrome.runtime.onInstalled` 이벤트에서 컨텍스트 메뉴 2개 등록
- `chrome.contextMenus.onClicked` 이벤트에서 클릭된 메뉴 구분
- 현재 탭의 title, url 수집 후 `chrome.storage.local`에 배열로 저장
- 기존 데이터 불러오기 → 새 항목 추가 → 다시 저장 (배열 push 방식)

**popup.js**

- 팝업 로드 시 `chrome.storage.local.get()`으로 데이터 불러오기
- TODO / 읽기 목록 각각 동적으로 `<li>` 생성하여 렌더링
- 체크박스 클릭 → 완료 여부 토글 후 storage 업데이트
- 삭제 버튼 클릭 → 해당 항목 배열에서 제거 후 storage 업데이트
- 전체 삭제 → 해당 탭 배열 초기화
- 제목 클릭 → `window.open(url)`으로 새 탭 열기

### 4-5. manifest.json 권한 설정

```json
{
  "manifest_version": 3,
  "name": "QuickSave",
  "version": "1.0",
  "description": "우클릭으로 할 일과 읽기 목록을 즉시 저장하는 익스텐션",
  "permissions": ["contextMenus", "storage", "tabs", "scripting"],
  "background": {
    "service_worker": "background.js"
  },
  "action": {
    "default_popup": "popup.html",
    "default_icon": {
      "16": "icons/icon16.png",
      "48": "icons/icon48.png",
      "128": "icons/icon128.png"
    }
  },
  "icons": {
    "16": "icons/icon16.png",
    "48": "icons/icon48.png",
    "128": "icons/icon128.png"
  }
}
```

---

## 📅 개발 일정

| 일차         | 작업 내용                                         |
| ------------ | ------------------------------------------------- |
| 1일차 (오늘) | 기획서 작성 완료, manifest.json 및 파일 구조 세팅 |
| 2일차        | background.js 완성 (우클릭 메뉴 + 저장 로직)      |
| 3일차        | popup.html / popup.css / popup.js 완성 (UI 구현)  |
| 4일차        | 전체 테스트, 버그 수정, 최종 제출                 |

---

_본 기획서는 개발 진행에 따라 수정될 수 있습니다._
